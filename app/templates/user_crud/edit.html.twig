{% extends 'base.html.twig' %}

{% block title %}Editar Usuario{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center" style="margin-top: 20px;">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h1 class="h3 mb-4">Editar usuario</h1>

                    {% include 'partials/_alert_messages.html.twig' %}
                    <div class="mb-4">
                        {{ include('user_crud/_form.html.twig', { 'button_label': 'Update'}) }}
                    </div>

                    <div class="d-flex justify-content-between align-items-center">
                        <a href="{{ path('app_user_crud_index') }}" class="btn btn-outline-primary">
                        ← Volver al listado
                        </a>
                        {{ include('user_crud/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
