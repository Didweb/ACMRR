{% extends 'base.html.twig' %}

{% block title %}ProductTag{% endblock %}

{% block body %}
    <h1>ProductTag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_product_tag_index') }}">back to list</a>

    <a href="{{ path('app_product_product_tag_edit', {'id': product_tag.id}) }}">edit</a>

    {{ include('product/product_tag/_delete_form.html.twig') }}
{% endblock %}
