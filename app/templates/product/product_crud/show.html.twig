{% extends 'base.html.twig' %}

{% block title %}ProductTitle{% endblock %}

{% block body %}
    <h1>ProductTitle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_title.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_title.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_crud_index') }}">back to list</a>

    <a href="{{ path('app_product_crud_edit', {'id': product_title.id}) }}">edit</a>

    {{ include('product/product_crud/_delete_form.html.twig') }}
{% endblock %}
