<form method="post" action="{{ path('app_riddim_delete', {'id': riddim.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ riddim.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
