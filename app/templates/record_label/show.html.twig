{% extends 'base.html.twig' %}

{% block title %}RecordLabel{% endblock %}

{% block body %}
    <h1>RecordLabel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ record_label.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ record_label.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_record_label_index') }}">back to list</a>

    <a href="{{ path('app_record_label_edit', {'id': record_label.id}) }}">edit</a>

    {{ include('record_label/_delete_form.html.twig') }}
{% endblock %}
