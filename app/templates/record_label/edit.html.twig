{% extends 'base.html.twig' %}

{% block title %}Edit RecordLabel{% endblock %}

{% block body %}
    <h1>Edit RecordLabel</h1>

    {{ include('record_label/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_record_label_index') }}">back to list</a>

    {{ include('record_label/_delete_form.html.twig') }}
{% endblock %}
