{% extends 'base.html.twig' %}

{% block title %}Nuevo Sello{% endblock %}

{% block body %}
<div class="container">
<div class="row justify-content-center" style="margin-top: 20px;">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h1 class="h3 mb-4">Crear Sello</h1>

                <div class="mb-4">
                    {{ include('record_label/_form.html.twig') }}
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <a href="{{ path('app_record_label_index') }}" class="btn btn-outline-primary">← Volver al listado</a>
                </div>
                
            </div>
        </div>
    </div>
</div>
{% endblock %}
